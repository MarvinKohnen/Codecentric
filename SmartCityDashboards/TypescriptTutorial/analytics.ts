console.log('Sending...')

//'npm start' to bootup localhost 
//'tsc --init' to start watching all projects in this folder (TypescriptTutorial)
//then just 'tsc' to compile everything in folder


//Project Management Options:
//excluding and including (if we include, we have to specify EVERY file we want compilated) files in tsconfig.json

//debugging via VS Code, Watch vid again
